#pragma once
#include <PCH.hpp>

namespace Global {
    inline std::vector<std::string> leftWords;
    inline std::vector<std::string> rightWords;

    inline std::string contents;
    inline int length;
    inline uint16_t mode;
    inline char seperator;
}